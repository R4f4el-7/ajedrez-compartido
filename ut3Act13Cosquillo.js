let movimiento = 0;

for (let i = 0; i <= 10; i++) {
    console.log(`El número total de movimientos realizados en la partida es: ${movimiento}`);
    movimiento += 1;
}

//Se usa 'let' porque la variable 'movimiento' cambia su valor.
//Se emplea una plantilla literal (`) para crear un mensaje claro y legible.